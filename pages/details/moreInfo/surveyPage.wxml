
<view class='container'>

  <scroll-view class='showList' scroll-y="true" style="height: 400px">
    <block wx:for="{{show}}" wx:for-item="single_order" wx:key='id'>
      <view class="orderItem" catchtap='selected' data-bean="{{single_order}}">

        <block wx:if="{{single_order.type == 0}}">
          <view class='title'>{{single_order.id + "：" + single_order.title}}</view>
          <input class='input_answer'></input>
        </block>

        <!--block wx:if="{{single_order.type == 1}}">
          <view class='title'>{{single_order.id + "：" + single_order.title}}</view>
          <radio-group class="type-radio-group" bindchange="typeChange">
            <radio class="radio1" value="choice1" checked="{{single_order.res=='A'}}">{{"A：" + single_order.chocie1}}</radio>
            <radio class="radio2" value="choice2" checked="{{single_order.res=='B'}}">{{"B：" + single_order.chocie2}}</radio>
            <radio class="radio3" value="choice3" checked="{{single_order.res=='C'}}">{{"C：" + single_order.chocie3}}</radio>
            <radio class="radio4" value="choice4" checked="{{single_order.res=='D'}}">{{"D：" + single_order.chocie4}}</radio>
          </radio-group>
        </block>

        <block wx:if="{{single_order.type == 2}}">
          <view class='title'>{{single_order.id + "：" + single_order.title}}</view>
          
          <checkbox-group class="type-radio-group" bindchange="typeChange">
            <checkbox class="radio1" value="choice1">{{"A：" + single_order.chocie1}}</checkbox>
            <checkbox class="radio2" value="choice2">{{"B：" + single_order.chocie2}}</checkbox>
            <checkbox class="radio3" value="choice3">{{"C：" + single_order.chocie3}}</checkbox>
            <checkbox class="radio4" value="choice4">{{"D：" + single_order.chocie4}}</checkbox>
          </checkbox-group>
          
        </block-->

        <block wx:if="{{single_order.type == 1}}">
          <view class='title'>{{single_order.id + "：" + single_order.title}}</view>
          <radio-group class="type-radio-group" bindchange="typeChange">
            <block wx:for="{{single_order.choice}}" wx:for-item="single_choice">
              <radio class="radio1" value='{{single_choice.index}}' checked='{{single_choice.check}}'>{{single_choice.index + ": " + single_choice.title}}</radio>
            </block>
          </radio-group>
        </block>

        <block wx:if="{{single_order.type == 2}}">
          <view class='title'>{{single_order.id + "：" + single_order.title}}</view>
          <checkbox-group class="type-radio-group" bindchange="typeChange2">
            <block wx:for="{{single_order.choice}}" wx:for-item="single_choice" wx:key='cid'>
              <checkbox class="radio1" value='{{single_choice.index}}'>{{single_choice.index + ": " + single_choice.title}}</checkbox>
            </block>
          </checkbox-group>
        </block>

        <view class='divLine'></view>
  
      </view>
    </block>
  </scroll-view>

  <view class='finish_status'>{{"完 成 情 况：" + finish_num + " / " + total_num}}</view>

</view>

